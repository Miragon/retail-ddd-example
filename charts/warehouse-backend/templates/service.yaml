apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-backend.fullname" . }}
  labels:
    {{- include "warehouse-backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warehouse-backend.selectorLabels" . | nindent 4 }}
