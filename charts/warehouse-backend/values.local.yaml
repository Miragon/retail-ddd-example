image:
  pullPolicy: Never

ingress:
  enabled: true
  stripPrefix:
    enabled: true
    prefixes:
      - /warehouse
  hosts:
    - host: localhost
      paths:
        - path: /warehouse
          pathType: Prefix

env:
  - name: SPRING_PROFILES_ACTIVE
    value: "local"
  - name: SERVER_PORT
    value: "8080"
