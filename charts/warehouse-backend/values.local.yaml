image:
  pullPolicy: Never

ingress:
  enabled: true
  stripPrefix:
    enabled: true
    prefixes:
      - /warehouse
  hosts:
    - host: localhost
      paths:
        - path: /warehouse
          pathType: Prefix

env:
  - name: SWAGGER_API_ENABLED
    value: "false"
