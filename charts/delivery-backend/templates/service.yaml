apiVersion: v1
kind: Service
metadata:
  name: {{ include "delivery-backend.fullname" . }}
  labels:
    {{- include "delivery-backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "delivery-backend.selectorLabels" . | nindent 4 }}
