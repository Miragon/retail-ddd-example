traefik:
  service:
    type: LoadBalancer

  ports:
    web:
      exposedPort: 8080
    websecure:
      expose:
        default: false

  ingressClass:
    enabled: true
    isDefaultClass: false
    name: traefik
